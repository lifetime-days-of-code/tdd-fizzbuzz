
```
# Coverage measurement 

# --coverage -> this flag computes code coverage.
  (by default this saves a report to a coverage directory 
   and shows a summary in the console.)

# --coverageReporters=text -> this flag just print the summary 
  in the console

npm run test -- --coverage --coverageReporters=text

```

```
# starts jest in watch mode

npx jest --watchAll
```
